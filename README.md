# DataScienceProject1

This repository hosts my **first end-to-end data science project**, including a deployment-ready workflow.

## Project Goal

Build and deploy a complete data science solution that covers:

1. Data Ingestion
2. Data Validation
3. Data Preparation
4. Model Trainer
5. Model Evaluation

## Workflows

1. Update config.yaml - important configurations that we required (Data ingestion mean read the source of data from somewhere like from api using the ETL pipeline -> postgres load )
2. Update schema.yaml - checking the schema of the input we getting from source and it should be validated before we use it 
3. Update params.yaml - it used when we have to provide some paramaters
4. Update the entity - 
5. Update the configuration manager in src config
6. Update the components
7. Update the pipeline
8. Update the main.py
